# 🔍 Clone Analysis | Project: onnx | PR: #7112

- **Commit SHA:** `7d8728721b8dee4a35ac3c4fb6eaf1d00cf99a51`
- **Clone Fingerprint:** `b318f37f7de4cd90c0b9b891c757b92d`
- **Categoria:** `mei_final`

---

## 🧑‍💻 Clone Par 1
**File:** `onnx/backend/test/case/node/attention.py`
**Lines:** 67 to 88

```text
def export_attention_scaled() -> None:
        scale = 1e-2
        node = onnx.helper.make_node(
            "Attention",
            inputs=["Q", "K", "V"],
            outputs=["Y"],
            scale=scale,
        )

        Q = np.random.rand(2, 3, 4, 8).astype(np.float32)
        K = np.random.rand(2, 3, 6, 8).astype(np.float32)
        V = np.random.rand(2, 3, 6, 8).astype(np.float32)

        Y, _, _, _ = _compute_attention(Q, K, V, scale=scale)

        expect(
            node,
            inputs=[Q, K, V],
            outputs=[Y],
            name="test_attention_4d_scaled",
        )
```

---

## 🧑‍💻 Clone Par 2
**File:** `onnx/backend/test/case/node/attention.py`
**Lines:** 113 to 134

```text
def export_attention_diff_head_sizes_scaled() -> None:
        scale = 1e-2
        node = onnx.helper.make_node(
            "Attention",
            inputs=["Q", "K", "V"],
            outputs=["Y"],
            scale=scale,
        )

        Q = np.random.rand(2, 3, 4, 8).astype(np.float32)
        K = np.random.rand(2, 3, 6, 8).astype(np.float32)
        V = np.random.rand(2, 3, 6, 10).astype(np.float32)

        Y, _, _, _ = _compute_attention(Q, K, V, scale=scale)

        expect(
            node,
            inputs=[Q, K, V],
            outputs=[Y],
            name="test_attention_4d_diff_heads_sizes_scaled",
        )
```

