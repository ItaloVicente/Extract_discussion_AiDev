# 🔍 Clone Analysis | Project: kura | PR: #53

- **Commit SHA:** `dfa54dca830357011e6695b6af95ea23426753a0`
- **Clone Fingerprint:** `95264668ce6fdd5f8a87c7d99873b89c`
- **Categoria:** `mei`

---

## 🧑‍💻 Clone Par 1
**File:** `kura/explorer/backend/tests/test_search.py`
**Lines:** 174 to 184

```text
def test_search_explorer_not_initialized(self):
        """Test search endpoint when explorer is not initialized."""
        with patch('main.explorer', None):
            from main import app
            from fastapi.testclient import TestClient
            client = TestClient(app)
            
            response = client.get("/api/search?q=test")
            assert response.status_code == 503
            assert response.json()["detail"] == "Explorer not initialized"
```

---

## 🧑‍💻 Clone Par 2
**File:** `kura/explorer/backend/tests/test_conversations.py`
**Lines:** 100 to 111

```text
def test_get_conversations_explorer_not_initialized(self):
        """Test conversations endpoint when explorer is not initialized."""
        with patch('main.explorer', None):
            from main import app
            from fastapi.testclient import TestClient
            client = TestClient(app)
            
            response = client.get("/api/conversations")
            assert response.status_code == 503
            assert response.json()["detail"] == "Explorer not initialized"
```

