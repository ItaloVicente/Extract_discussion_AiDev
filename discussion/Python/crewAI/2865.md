# Discussões da PR #2865 | Projeto: crewAI

**🔗 Link da PR:** [https://github.com/crewAIInc/crewAI/pull/2865](https://github.com/crewAIInc/crewAI/pull/2865)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 2 |
| `pr_reviews` | 1 |
| `pr_review_comments` | 0 |

---

## 📝 Pull Request Body (Autor: devin-ai-integration[bot])

> # Add markdown attribute to Task class
> 
> This PR adds a new boolean attribute to the Task class called `markdown` that, when set to True, will automatically inject instructions into the task prompt to return the final answer formatted in Markdown.
> 
> ## Changes
> - Added `markdown` boolean attribute to the Task class with default=False
> - Modified the `prompt()` method to include markdown formatting instructions when markdown=True
> - Added a test to verify that when markdown=True, the prompt includes the markdown formatting instructions
> 
> ## Example Usage
> ```python
> task = Task(
>     description="Research advances in AI",
>     expected_output="A comprehensive summary",
>     markdown=True,
>     agent=researcher
> )
> ```
> 
> This will include an instruction in the prompt telling the agent to format its final answer in Markdown.
> 
> ## Testing
> - Added new test file: test_markdown_task.py with two test cases:
>   - test_markdown_option_in_task_prompt
>   - test_markdown_option_not_in_task_prompt_by_default
> 
> Note: There's a conflict between pytest-recording and pytest-vcr in the test environment that prevented running the tests locally. The implementation follows the existing patterns in the codebase and should work correctly once the environment issue is resolved.
> 
> Link to Devin run: https://app.devin.ai/sessions/4a9dee4334a74a16bb3b44d859a5461c
> Requested by: Joe Moura (joao@crewai.com)
> 

---

## 💬 PR Comments (2)

**👤 devin-ai-integration[bot]** comentou em 2025-05-20T05:40:26Z:

### 🤖 Devin AI Engineer

I'll be helping with this pull request! Here's what you should know:

✅ I will automatically:
- Address comments on this PR. Add '(aside)' to your comment to have me ignore it.
- Look at CI failures and help fix them

Note: I can only respond to comments from users who have write access to this repository.

⚙️ Control Options:
- [ ] Disable automatic comment and CI monitoring


**👤 joaomdmoura** comentou em 2025-05-20T05:42:23Z:

Disclaimer: This review was made by a crew of AI Agents.

# Code Review Comment for PR #2865: Markdown Attribute Addition

## Overview
This pull request introduces a new `markdown` attribute in the Task class, empowering agents to format their responses using Markdown syntax. This addition facilitates better output clarity and enhances user experience.

## Code Quality Findings
### src/crewai/task.py
- **Implementation Clarity**: The integration of the `markdown` feature is straightforward and enhances the overall functionality of the Task class.
- **Whitespace Consistency**: It's essential to adhere to uniform code style. For example, remove the extra blank line before the return statement in the `prompt` method to keep the formatting clean and consistent.
  ```python
  # Suggested Improvement:
  if self.markdown:
      markdown_instruction = "Your final answer MUST be formatted in Markdown syntax."
      tasks_slices.append(markdown_instruction)
  return "\n".join(tasks_slices)
  ```
- **Detailed Markdown Instructions**: Improving the user instructions by providing specifics on Markdown usage benefits user understanding. Adding practical examples of Markdown formatting would enrich the user guidance.
  ```python
  # Suggested Improvement:
  markdown_instruction = """Your final answer MUST be formatted in Markdown syntax.
  Follow these guidelines:
  - Use # for headers
  - Use ** for bold text
  - Use * for italic text
  - Use - or * for bullet points
  - Use `code` for inline code
  - Use ```language for code blocks"""
  ```

- **Enhanced Type Hints**: Enriching the method's type hints along with detailed docstrings will improve code readability and provide clear expectations for users.
  ```python
  # Suggested Improvement:
  def prompt(self) -> str:
      """Generates the task prompt with optional markdown formatting.
      
      Returns:
          str: The formatted prompt string
      """
  ```

### tests/test_markdown_task.py
- **Good Test Coverage**: Comprehensive testing for both situations where the `markdown` flag is active and inactive. However, the addition of edge test cases (e.g., empty descriptions) could thoroughly validate functionality.
- **Parameterized Tests**: Utilizing parameterized tests might streamline the testing process. Testing variations of the `markdown` flag would improve coverage and maintainability.
  
## Historical Context from Related PRs
While I currently lack access to related PR data, it is crucial that the evolution of the markdown features be referenced in future changes, especially if other components of the system depend on it. Incorporating lessons learned about user experience from past changes can further enhance implementation strategies.

## General Recommendations
1. **Documentation Updates**: Further elaborate on documentation for the Task class regarding Markdown formatting and include usage examples. This would guide users and ensure clarity across implementations.
2. **Error Handling**: Implement checks and validations related to markdown compatibility with other types of formatting or outputs to safeguard against runtime errors and ensure stability.
3. **Configurable Inputs**: Making the guidelines and messages flexible by allowing configurations would accommodate various user needs.
4. **Continued Performance Evaluation**: It should be verified that the addition of Markdown handling does not adversely affect the performance of the Task class, especially under heavy loads.

## Conclusion
Overall, PR #2865 is a valuable contribution that enhances the agent's output flexibility while improving user engagement through clearer formatting. The reviewed suggestions should enhance usability, maintainability, and documentation, further solidifying the benefits of this feature.

---

## 🔍 PR Reviews (1) e Comentários de Linha (0)

### 🛠️ Revisão por joaomdmoura [Status: APPROVED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

*(Nenhum comentário de linha atrelado a esta revisão)*

---

