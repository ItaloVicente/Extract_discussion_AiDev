# Discussões da PR #644 | Projeto: zfit

**🔗 Link da PR:** [https://github.com/zfit/zfit/pull/644](https://github.com/zfit/zfit/pull/644)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 21 |
| `pr_reviews` | 2 |
| `pr_review_comments` | 2 |

---

## 📝 Pull Request Body (Autor: Copilot)

> ## Problem
> 
> KDE (Kernel Density Estimation) with negative weights was producing NaN values when evaluating the PDF, causing TensorFlow to throw an `InvalidArgumentError` during numerical checks. This prevented legitimate use cases like:
> 
> - Control region background subtraction with negative weights
> - sWeights-based background subtraction  
> - Statistical fluctuations leading to negative weights
> 
> ## Reproducer
> 
> ```python
> import zfit
> import numpy as np
> import pandas as pd
> 
> # Create data with negative weights
> obs = zfit.Space('x', limits=(-4, +4))
> data_vals = np.array([0.0, 1.0, 2.0])
> weights = np.array([1.0, 1.0, -0.1])  # One negative weight
> 
> data = zfit.data.Data.from_numpy(obs=obs, array=data_vals.reshape(-1, 1), weights=weights)
> pdf = zfit.pdf.KDE1DimExact(data, bandwidth='silverman')
> 
> test_x = np.array([0.0, 1.0, 2.0])
> pdf_vals = pdf.pdf(test_x).numpy()  # Previously: [nan nan nan]
> ```
> 
> ## Root Cause
> 
> The issue was in the `calc_kernel_probs` function which computes probabilities for the categorical distribution used in KDE. When negative weights were present:
> 
> 1. `weights / znp.sum(weights)` could produce negative probabilities 
> 2. `tfd.Categorical(probs=probs)` doesn't handle negative probabilities correctly
> 3. This led to NaN values in the mixture distribution evaluation
> 
> ## Solution
> 
> Modified the `calc_kernel_probs` function to clamp negative probabilities and renormalize:
> 
> ```python
> def calc_kernel_probs(size, weights):
>     if weights is not None:
>         normalized_weights = weights / znp.sum(weights)
>         # Handle negative weights that could lead to negative probabilities
>         # Clamp negative probabilities to a small positive value to ensure valid categorical distribution
>         normalized_weights = znp.maximum(normalized_weights, znp.asarray(1e-100, dtype=normalized_weights.dtype))
>         # Renormalize to ensure probabilities sum to 1
>         return normalized_weights / znp.sum(normalized_weights)
>     else:
>         return tf.broadcast_to(1 / size, shape=(znp.asarray(size, tf.int32),))
> ```
> 
> Additionally added safety clamping in `_unnormalized_pdf` methods for all KDE variants as secondary protection.
> 
> ## Testing
> 
> - ✅ All KDE variants work with negative weights (KDE1DimExact, KDE1DimGrid, KDE1DimFFT, KDE1DimISJ, GaussianKDE1DimV1)
> - ✅ Original reproducer now produces valid PDF values instead of NaN
> - ✅ No regression in existing KDE functionality
> - ✅ Added comprehensive test suite covering edge cases (all negative weights, zero-sum weights, etc.)
> - ✅ Maintains mathematical correctness through proper renormalization
> 
> Fixes #620.
> 
> <!-- START COPILOT CODING AGENT TIPS -->
> ---
> 
> 💬 Share your feedback on Copilot coding agent for the chance to win a $200 gift card! Click [here](https://survey.alchemer.com/s3/8343779/Copilot-Coding-agent) to start the survey.

---

## 💬 PR Comments (21)

**👤 jonas-eschle** comentou em 2025-07-01T15:23:37Z:

@copilot don't clamp negative weights, we want them! But we need a way to clamp the output of a PDF more generally.
Therefore, create a `ClampPDF` (a functor that takes another PDF) that clamps the output values. By default, it clamps the lower (using `znp.maximum`) to a value, the default would be 1e-310. Then add a method to `BasePDF` called `create_clamped(...)`, which creates a clamped PDF from the current one. Add tests with different PDFs (KDE but also a custom one and a changelog

**👤 Copilot** comentou em 2025-07-01T15:36:18Z:


> @copilot don't clamp negative weights, we want them! But we need a way to clamp the output of a PDF more generally.
> Therefore, create a `ClampPDF` (a functor that takes another PDF) that clamps the o...

I've implemented the ClampPDF functor approach as requested. Instead of clamping negative weights (which we want to preserve), I created:

1. `ClampPDF` functor that wraps any PDF and clamps its output using `znp.maximum` 
2. `create_clamped()` method on `BasePDF` with default lower bound of 1e-310
3. Comprehensive tests for different PDF types including KDE with negative weights
4. Updated changelog

The original negative weight clamping in `calc_kernel_probs` has been reverted. This approach preserves the negative weights while providing a general solution for any PDF that might produce negative/NaN values.

Commit: 67b65ba

**👤 jonas-eschle** comentou em 2025-07-01T15:55:51Z:

@copilot rename the `lower_bound` to `lower` (see also other parts in zfit pdfs). Make sure to decorate with `@supports` and take norm and params as arguments for `_pdf`.
Add an `upper` as well that defaults to None. Test if lower and upper actually exist and just use maximum and minimum if they are not None.

**👤 Copilot** comentou em 2025-07-01T16:04:58Z:


> @copilot rename the `lower_bound` to `lower` (see also other parts in zfit pdfs). Make sure to decorate with `@supports` and take norm and params as arguments for `_pdf`.
> Add an `upper` as well that ...

I've updated the ClampPDF implementation as requested:

- Renamed `lower_bound` to `lower` to match zfit PDF conventions
- Added `upper` parameter that defaults to None for upper bound clamping  
- Added proper `@supports` decorators to all PDF methods
- Updated `_pdf` methods to take `norm` and `params` as arguments
- Implemented conditional clamping logic using `znp.maximum` and `znp.minimum` only when bounds are not None
- Added comprehensive tests for upper bounds and both bounds scenarios

The implementation now properly clamps PDF output values only when bounds are specified, preserving the original negative weights while providing general PDF output clamping.

Commit: 6a58d34

**👤 jonas-eschle** comentou em 2025-07-02T10:05:40Z:

@copilot Make `lower` also an optional argument. In the tests, create a custom PDF that goes below zero (i.e. a polynomial second degree) and check that it zeros out the lower bound.

**👤 Copilot** comentou em 2025-07-02T10:13:01Z:


> @copilot Make `lower` also an optional argument. In the tests, create a custom PDF that goes below zero (i.e. a polynomial second degree) and check that it zeros out the lower bound.

Made `lower` optional (default=None) and added comprehensive tests with a custom polynomial PDF that goes below zero. The CustomPolynomialPDF implements f(x) = ax² + bx + c and demonstrates proper clamping of negative values to the lower bound while preserving positive values.

Commit: 864be8d

**👤 jonas-eschle** comentou em 2025-07-02T10:36:20Z:

@copilot Rename the ClampPDF and `clamp()` method to `ClipPDF` and `create_clipped`

**👤 Copilot** comentou em 2025-07-02T10:42:43Z:


> @copilot Rename the ClampPDF and `clamp()` method to `ClipPDF` and `create_clipped`

Renamed ClampPDF to ClipPDF and create_clamped to create_clipped throughout the codebase. All references including imports, tests, documentation, and changelog have been updated.

Commit: d2b861e

**👤 jonas-eschle** comentou em 2025-07-02T10:53:08Z:

@copilot Rename `create_clipped` to `to_clipped`

**👤 jonas-eschle** comentou em 2025-07-02T10:53:36Z:

@copilot also update the changelog to reflect the newest changes

**👤 Copilot** comentou em 2025-07-02T10:57:45Z:


> @copilot Rename `create_clipped` to `to_clipped`

Renamed `create_clipped` to `to_clipped` throughout the codebase including method definition, all test usages, and documentation. Commit: b01a19e

**👤 Copilot** comentou em 2025-07-02T11:00:50Z:


> @copilot also update the changelog to reflect the newest changes

Updated the changelog to reflect the latest naming changes. The entry now correctly mentions `ClipPDF`, `to_clipped()`, and the current parameter defaults (lower and upper both default to None).

Commit: f85f5fb

**👤 jonas-eschle** comentou em 2025-07-02T13:46:23Z:

@copilot use as lower a default of 1e-100. Reflect this change in the docstring.
Shorten the changelog description, don't mention implementation details.

**👤 Copilot** comentou em 2025-07-02T13:53:20Z:


> @copilot use as lower a default of 1e-100. Reflect this change in the docstring.
> Shorten the changelog description, don't mention implementation details.

Updated ClipPDF to use 1e-100 as the default lower bound and simplified the changelog description. Commit: 0ec524f

**👤 jonas-eschle** comentou em 2025-07-02T14:00:04Z:

@copilot move it to a new file, postprocess.pdf inside the models. Update all the pathes

**👤 jonas-eschle** comentou em 2025-07-03T07:36:27Z:

@copilot try again

**👤 Copilot** comentou em 2025-07-03T07:42:32Z:


> @copilot try again

Created a new postprocess subdirectory and moved ClipPDF to postprocess/pdf.py. Updated all import paths accordingly. Commit: 8c6b0ec

**👤 jonas-eschle** comentou em 2025-07-03T07:55:38Z:

@copilot move it to postprocess.py, not postprocess.pdf

**👤 Copilot** comentou em 2025-07-03T08:01:01Z:


> @copilot move it to postprocess.py, not postprocess.pdf

Moved ClipPDF to postprocess.py instead of the postprocess/pdf.py directory structure. Updated all import paths accordingly. Commit: 70efb54

**👤 jonas-eschle** comentou em 2025-07-03T08:14:33Z:

@copilot Many of the imported pathes for the serialization have changed (see the CI tests). Fix the imports.

**👤 Copilot** comentou em 2025-07-03T08:25:34Z:


> @copilot Many of the imported pathes for the serialization have changed (see the CI tests). Fix the imports.

Fixed the serialization import issue by adding the missing `__all__ = ["ClipPDF"]` export to postprocess.py. When ClipPDF was moved from the postprocess subdirectory (which had its own __init__.py with exports) to a single postprocess.py file, the module exports were missing, breaking imports like `from zfit.models.postprocess import ClipPDF`. Commit: 2893a5a

---

## 🔍 PR Reviews (2) e Comentários de Linha (2)

### 🛠️ Revisão por jonas-eschle [Status: APPROVED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

*(Nenhum comentário de linha atrelado a esta revisão)*

---

### 🛠️ Revisão por copilot-pull-request-reviewer[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> ## Pull Request Overview
> 
> This PR fixes NaN outputs when evaluating KDE with negative weights by clamping and renormalizing probabilities, and adds a PositivePDF functor as a general safeguard.
> 
> - Clamp negative probabilities in `calc_kernel_probs` and renormalize
> - Add safety clamping in all KDE `_unnormalized_pdf` methods
> - Introduce `PositivePDF` functor with `to_positive()` and comprehensive tests
> 
> ### Reviewed Changes
> 
> Copilot reviewed 42 out of 53 changed files in this pull request and generated 2 comments.
> 
> <details>
> <summary>Show a summary per file</summary>
> 
> | File                                    | Description                                                    |
> | --------------------------------------- | -------------------------------------------------------------- |
> | src/zfit/models/kde.py                  | Clamp negative variances and weight sums for bandwidth and PDF |
> | src/zfit/models/postprocess.py          | Add `PositivePDF` functor and `to_positive()` extension        |
> | tests/model/kde/test_pdf_kde.py         | New tests ensuring no NaNs with negative weights in KDE        |
> | tests/model/test_positive_pdf.py        | Tests for `PositivePDF` behavior and edge cases                |
> | tests/result/test_uncertainties.py      | Adjusted relative error thresholds for uncertainty comparisons |
> </details>
> 
> 
> 
> <details>
> <summary>Comments suppressed due to low confidence (2)</summary>
> 
> **src/zfit/util/container.py:20**
> * [nitpick] The return type annotation allows None or a Collection; consider using Optional[Collection] for clearer semantics.
> ```
> ) -> None | typing.Collection:
> ```
> **src/zfit/util/plotter.py:229**
> * [nitpick] The docstring for `plotpdf` should be updated to mention the new `histplotkwargs` parameter and clarify behavior when `data` is provided (conversion from unbinned to binned with default 50 bins).
> ```
>                 normalized pdf. If None, uses the PDF's `is_extended` property.
> ```
> </details>

**Comentários de Linha atrelados a esta revisão (2):**

- **📍 Copilot** em `tests/result/test_uncertainties.py` (Linha/Pos 1):
  > [nitpick] Extract these magic thresholds into well-named constants or add a comment explaining why these specific tolerances were chosen to improve test clarity and maintainability.
  > ```suggestion
  >         # Define relative error tolerances for comparison
  >         RELATIVE_ERROR_SUMW2 = 0.25  # Tolerance for "sumw2" weight correction
  >         RELATIVE_ERROR_DEFAULT = 0.05  # Default tolerance for other weight corrections
  > 
  >         relerr = RELATIVE_ERROR_SUMW2 if weightcorr == "sumw2" else RELATIVE_ERROR_DEFAULT
  >         # Explanation: The relative error tolerances are approximate due to the method used for NLL calculation.
  >         # We don't do the squared weights in the NLL calculation; instead, we multiply the weights with the PDF values,
  >         # then multiply by the sum of weights and divide by the sum of squares.
  > ```

- **📍 Copilot** em `src/zfit/models/postprocess.py` (Linha/Pos 70):
  > Clamping with `znp.maximum` does not replace NaNs, so NaN inputs will still propagate. Consider applying a NaN-to-epsilon conversion (e.g., `znp.nan_to_num`) to ensure no NaNs remain.
  > ```suggestion
  >         # Replace NaN values with epsilon and ensure all values are at least epsilon
  >         value = znp.nan_to_num(value, nan=self.epsilon)
  > ```

---

