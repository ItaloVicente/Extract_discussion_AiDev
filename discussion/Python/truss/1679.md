# Discussões da PR #1679 | Projeto: truss

**🔗 Link da PR:** [https://github.com/basetenlabs/truss/pull/1679](https://github.com/basetenlabs/truss/pull/1679)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 0 |
| `pr_reviews` | 5 |
| `pr_review_comments` | 8 |

---

## 📝 Pull Request Body (Autor: marius-baseten)

> *(Sem descrição fornecida)*

---

## 💬 PR Comments (0)

*Nenhum comentário geral registrado.*

---

## 🔍 PR Reviews (5) e Comentários de Linha (8)

### 🛠️ Revisão por tyranitar [Status: APPROVED]

**Comentário Geral da Revisão:**
> Looks good.

**Comentários de Linha atrelados a esta revisão (2):**

- **📍 tyranitar** em `truss/base/truss_config.py` (Linha/Pos 1):
  > Nit:
  > ```suggestion
  >             self.is_websocket_endpoint
  > ```

- **📍 tyranitar** em `truss/templates/control/control/server.py` (Linha/Pos 1):
  > ```suggestion
  >             if transport and transport.get("kind") == "websocket":
  > ```

---

### 🛠️ Revisão por ndeepak-baseten [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (3):**

- **📍 ndeepak-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 17):
  > @marius-baseten Perhaps make this return a boolean type, `True` if validation successful and `False` otherwise. To make sense where called, we could rename it to `_valid_transport_options()`.

- **📍 ndeepak-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 19):
  > Is a blank return OK here? Would a debug or info log add value?

- **📍 ndeepak-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 21):
  > The pattern here leaves a few conditions unhandled.
  > ```
  > if ...
  >   if not ...
  >      # handled
  >   else:
  >      # not handled
  > else:
  >   if ...:
  >     # handled
  >   else:
  >     # not handled
  > ```
  > 
  > Is this OK? For clarity, we could also simply add a comment for the not-handled situations with a `pass` statement.

---

### 🛠️ Revisão por marius-baseten [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 marius-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 17):
  > There is a pattern in this file to use `_collect_error` and not return anything (there is nothing to handle the return values in a meaningful way). And all the other methods are also named `_validate_{X}` so I'm going to leave this.

---

### 🛠️ Revisão por marius-baseten [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 marius-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 19):
  > Yes, it just means the validation completed early in this case.

---

### 🛠️ Revisão por marius-baseten [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 marius-baseten** em `truss-chains/truss_chains/framework.py` (Linha/Pos 21):
  > The first "not handled" means the descriptor is websocket and the transport is for websocket - what do we need to handle? We don't have to validate anything in the pydantic model itself, we just assure that the kind of model matches the kind of endpoint.
  > 
  > The second one is bit more vague, it would make sense to replace `is_websocket`  with a kind-enum if we had more other options like GRPC. At the moment not websocket just implies HTTP. I'll add a TODO for considering making this explicit.

---

