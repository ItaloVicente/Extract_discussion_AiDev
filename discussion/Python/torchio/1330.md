# Discussões da PR #1330 | Projeto: torchio

**🔗 Link da PR:** [https://github.com/TorchIO-project/torchio/pull/1330](https://github.com/TorchIO-project/torchio/pull/1330)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 6 |
| `pr_reviews` | 20 |
| `pr_review_comments` | 34 |

---

## 📝 Pull Request Body (Autor: Copilot)

> ## Summary
> 
> This PR adds a new `default_pad_label` parameter to `RandomAffine` and `Affine` transforms, allowing users to specify custom padding values for label maps during spatial transformations.
> 
> ## Problem
> 
> Previously, the `default_pad_value` parameter was ignored for label maps (images of type `LABEL`), which always used a hardcoded padding value of 0. This prevented users from setting custom background values for transformed label maps:
> 
> ```python
> import torchio as tio
> import torch
> import numpy as np
> 
> # Create a simple label map
> label_data = torch.from_numpy(np.full((1, 2, 2, 2), 1))
> subject = tio.Subject(label=tio.LabelMap(tensor=label_data))
> 
> # This didn't work - default_pad_value was ignored for labels
> transform = tio.RandomAffine(
>     translation=(-10, 10, -10, 10, -10, 10),
>     default_pad_value=250  # Ignored for label maps!
> )
> result = transform(subject)
> print(result['label'].tensor)  # Shows 0s instead of 250s
> ```
> 
> ## Solution
> 
> Added a new `default_pad_label` parameter that:
> 
> - **Works for label maps**: Allows custom padding values for `LABEL` type images
> - **Supports all modes**: 'minimum', 'mean', 'otsu', and numeric values (same as `default_pad_value`)
> - **Maintains backward compatibility**: Defaults to 0, so existing code works unchanged
> - **Separates concerns**: Intensity images use `default_pad_value`, label maps use `default_pad_label`
> 
> ## Usage
> 
> ```python
> # Basic usage - custom label padding
> transform = tio.RandomAffine(default_pad_label=250)
> 
> # Mixed subjects - different padding for different image types
> transform = tio.RandomAffine(
>     default_pad_value=100,   # For intensity images
>     default_pad_label=200    # For label maps
> )
> 
> # String modes supported
> transform = tio.RandomAffine(default_pad_label='minimum')
> 
> # Works with direct Affine class too
> affine = tio.Affine(
>     scales=(1, 1, 1),
>     degrees=(0, 0, 0), 
>     translation=(5, 0, 0),
>     default_pad_label=123
> )
> ```
> 
> ## Changes Made
> 
> 1. **Added `default_pad_label` parameter** to both `RandomAffine` and `Affine` constructors with default value of 0
> 2. **Implemented `get_default_pad_label()` method** that mirrors `get_default_pad_value()` functionality
> 3. **Modified transform logic** to use `get_default_pad_label()` for non-intensity images instead of hardcoded 0
> 4. **Added comprehensive documentation** explaining the new parameter and its relationship to `default_pad_value`
> 5. **Added thorough test coverage** including edge cases, mixed subjects, and all supported modes
> 6. **Maintained perfect backward compatibility** - all existing code continues to work exactly as before
> 
> ## Backward Compatibility
> 
> ✅ **Fully backward compatible** - existing code using `default_pad_value` continues to work exactly as before. Label maps still use 0 as the default padding value unless explicitly overridden with `default_pad_label`.
> 
> ## Testing
> 
> - Added comprehensive test cases covering all functionality
> - Verified edge cases (mixed subjects, multiple label maps, string modes)
> - Confirmed existing tests continue to pass
> - Validated the exact scenario from the original issue report
> 
> Fixes #1304.
> 
> <!-- START COPILOT CODING AGENT TIPS -->
> ---
> 
> 💡 You can make Copilot smarter by setting up custom instructions, customizing its development environment and configuring Model Context Protocol (MCP) servers. Learn more [Copilot coding agent tips](https://gh.io/copilot-coding-agent-tips) in the docs.

---

## 💬 PR Comments (6)

**👤 fepegar** comentou em 2025-06-28T18:19:31Z:

Do not replicate the behaviour of `default_pad_value`. The only accepted types for this kwarg are `int` or `float`, that's all.

**👤 Copilot** comentou em 2025-06-28T18:34:29Z:


> Do not replicate the behaviour of `default_pad_value`. The only accepted types for this kwarg are `int` or `float`, that's all.

Simplified the `default_pad_label` parameter to only accept `int` or `float` values. Removed all string mode support ('minimum', 'mean', 'otsu') and the `get_default_pad_label()` method. The parameter now uses direct numeric value assignment with clear validation that rejects string values. Commit 7a6325f

**👤 fepegar** comentou em 2025-06-28T20:37:02Z:

Fix the pre-commit issues. Run with `pip install uv && uv run --all-groups pre-commit run --all-files`. Run the tox environments before pushing.

**👤 Copilot** comentou em 2025-06-28T20:43:30Z:


> Fix the pre-commit issues. Run with `pip install uv && uv run --all-groups pre-commit run --all-files`. Run the tox environments before pushing.

Fixed the pre-commit issues. All pre-commit hooks and tox environments (lint, format) now pass successfully. Commit 7a6325f

**👤 fepegar** comentou em 2025-06-28T21:41:55Z:

@romainVala FYI 😄 

**👤 romainVala** comentou em 2025-06-30T05:53:29Z:

Amazing !

---

## 🔍 PR Reviews (20) e Comentários de Linha (34)

### 🛠️ Revisão por fepegar [Status: CHANGES_REQUESTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (11):**

- **📍 fepegar** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > ```suggestion
  >             message = f'default_pad_label must be a number, but it is "{default_pad_label}"'
  >             raise ValueError(message)
  > ```

- **📍 fepegar** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Don't mention the default values in docstrings.

- **📍 fepegar** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > As above.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Use integers.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > ```suggestion
  >             translation=(10, 10), default_pad_label=250,
  > ```

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > ```suggestion
  >         s_aug = aff(s)
  > ```

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Call it `transform`.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Call it `subject`.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Instantiate the message before.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 24):
  > As above.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Use `.data`.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Updated the error message format to use quotes around the value. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Removed mention of default values from the docstring. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Removed mention of default values from the docstring. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed the test to use integer dtype (torch.int32). Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Simplified the translation parameter format. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed variable name to `transformed_subject`. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed variable name to `transform`. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed variable name to `subject`. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Instantiated the message before the assertion. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 24):
  > Instantiated the message before the assertion. Commit 92e622f

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed to use `.data` instead of `.tensor`. Commit 92e622f

---

### 🛠️ Revisão por fepegar [Status: CHANGES_REQUESTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (6):**

- **📍 fepegar** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Split in two lines

- **📍 fepegar** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > As above.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Use torch.ones and just don't specify the dtype.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Don't set `p`.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > As above.

- **📍 fepegar** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 25):
  > As above.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Split the error message into two lines as requested. Commit 878c804

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `src/torchio/transforms/augmentation/spatial/random_affine.py` (Linha/Pos 1):
  > Split the error message into two lines for the Affine class as well. Commit 878c804

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Changed to use `torch.ones` without specifying dtype. Commit 878c804

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Removed the `p` parameter from the test. Commit 878c804

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 1):
  > Removed the `p` parameter from this test as well. Commit 878c804

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `tests/transforms/augmentation/test_random_affine.py` (Linha/Pos 25):
  > Fixed the test parameter as requested. Commit 878c804

---

### 🛠️ Revisão por fepegar [Status: APPROVED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

*(Nenhum comentário de linha atrelado a esta revisão)*

---

