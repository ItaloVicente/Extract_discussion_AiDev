# Discussões da PR #16909 | Projeto: mlflow

**🔗 Link da PR:** [https://github.com/mlflow/mlflow/pull/16909](https://github.com/mlflow/mlflow/pull/16909)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 1 |
| `pr_reviews` | 2 |
| `pr_review_comments` | 0 |

---

## 📝 Pull Request Body (Autor: Copilot)

> The `D417` rule (undocumented-param) was enabled in both Ruff and our custom linter (clint), causing redundant warnings and maintenance overhead. This PR removes the duplication by:
> 
> 1. **Removing D417 from Ruff configuration** - Deleted `"D417"` from the `select` list in `[tool.ruff.lint]` section of `pyproject.toml`
> 
> 2. **Cleaning up suppression comments** - Removed `# noqa: D417` comments from 32 files across the codebase while preserving:
>    - Other noqa codes in multi-code comments (e.g., `# noqa: F401, D417` → `# noqa: F401`)
>    - All existing `# clint: disable=missing-docstring-param` comments
> 
> The custom clint linter will continue to enforce parameter documentation requirements through its `missing-docstring-param` rule, maintaining the same code quality standards without duplication.
> 
> **Before:**
> ```python
> def example_function(param1, param2):  # noqa: D417
>     """Function with missing param docs."""
>     pass
> ```
> 
> **After:**
> ```python  
> def example_function(param1, param2):
>     """Function with missing param docs."""
>     pass
> ```
> 
> Fixes #16908.
> 
> <!-- START COPILOT CODING AGENT TIPS -->
> ---
> 
> 💡 You can make Copilot smarter by setting up custom instructions, customizing its development environment and configuring Model Context Protocol (MCP) servers. Learn more [Copilot coding agent tips](https://gh.io/copilot-coding-agent-tips) in the docs.

---

## 💬 PR Comments (1)

**👤 github-actions[bot]** comentou em 2025-07-28T05:53:50Z:

@Copilot Thank you for the contribution! Could you fix the following issue(s)?

#### &#x26a0; Invalid PR template

This PR does not appear to have been filed using the MLflow PR template. Please copy the PR template from [here](https://raw.githubusercontent.com/mlflow/mlflow/master/.github/pull_request_template.md) and fill it out.

---

## 🔍 PR Reviews (2) e Comentários de Linha (0)

### 🛠️ Revisão por harupy [Status: APPROVED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

*(Nenhum comentário de linha atrelado a esta revisão)*

---

### 🛠️ Revisão por harupy [Status: APPROVED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

*(Nenhum comentário de linha atrelado a esta revisão)*

---

