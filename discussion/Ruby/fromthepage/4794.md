# Discussões da PR #4794 | Projeto: fromthepage

**🔗 Link da PR:** [https://github.com/benwbrum/fromthepage/pull/4794](https://github.com/benwbrum/fromthepage/pull/4794)

## 📊 Resumo de Interações

| Origem da Tabela | Quantidade de Registros |
| :--- | :--- |
| `pull_request` (Body) | 1 |
| `pr_comments` | 8 |
| `pr_reviews` | 15 |
| `pr_review_comments` | 28 |

---

## 📝 Pull Request Body (Autor: Copilot)

> The WayBack Machine was not archiving FromThePage transcripts because the site lacked proper crawler guidance. Most transcript pages weren't being discovered due to complex nested URL structures (e.g., `/:user_slug/:collection_id/:work_id/display/:page_id`) and missing sitemap infrastructure.
> 
> ## Changes Made
> 
> ### 1. Dynamic Sitemap Generation
> - **New controller**: `SitemapController` with endpoints for `/sitemap.xml`, `/sitemap_collections.xml`, `/sitemap_works.xml`, and `/sitemap_pages.xml`
> - **Privacy-aware**: Only includes public collections (`restricted: false, is_active: true`)
> - **Content filtering**: Excludes blank pages from page sitemaps
> - **Standard compliance**: Proper XML formatting with `lastmod`, `changefreq`, and `priority` values
> 
> ### 2. Enhanced robots.txt
> Updated `/public/robots.txt` to include:
> ```
> # Allow crawling of transcripts and other content for archival purposes
> Allow: /*/display/*
> Allow: /*/*/*/display/*
> 
> # Sitemap location for better content discovery
> Sitemap: https://fromthepage.com/sitemap.xml
> ```
> 
> ### 3. SEO Metadata and Structured Data
> - **Meta tags**: Added descriptive titles, descriptions, and keywords to transcript, work, and collection pages
> - **Structured data**: JSON-LD markup following schema.org standards (DigitalDocument, Book, Collection)
> - **Archive headers**: Added `X-Robots-Tag: index, follow, archive` for better archival guidance
> 
> ### 4. Static Sitemap Generation (Rake Task)
> For large installations, added `rake sitemap:generate` task that:
> - Generates static XML files in `/public/sitemaps/`
> - Handles pagination (50,000 URLs per file)
> - Creates comprehensive sitemap index
> 
> ### 5. Testing and Documentation
> - **RSpec tests**: Controller tests validating XML structure and privacy controls
> - **XML validation**: Scripts to verify sitemap compliance
> - **Documentation**: Comprehensive guide in `/doc/WAYBACK_MACHINE_SUPPORT.md`
> 
> ## Example URLs Now Discoverable
> With the sitemap, crawlers can now discover URLs like:
> - `https://fromthepage.com/benwbrum/test-collection/sample-diary/display/12345`
> - `https://fromthepage.com/libraries/historical-letters/civil-war-correspondence/display/67890`
> 
> These deeply nested transcript URLs were previously undiscoverable by automated crawlers.
> 
> ## Impact
> The WayBack Machine and other archival crawlers will now:
> 1. Discover the sitemap via robots.txt reference
> 2. Follow sitemap links to find all public transcript pages
> 3. Better understand content through structured data
> 4. Archive transcripts more reliably and frequently
> 
> Fixes #1335.
> 
> <!-- START COPILOT CODING AGENT TIPS -->
> ---
> 
> 💬 Share your feedback on Copilot coding agent for the chance to win a $200 gift card! Click [here](https://survey.alchemer.com/s3/8343779/Copilot-Coding-agent) to start the survey.

---

## 💬 PR Comments (8)

**👤 benwbrum** comentou em 2025-07-26T13:51:24Z:

Please fix the failing spec tests.

**👤 Copilot** comentou em 2025-07-26T14:15:56Z:


> Please fix the failing spec tests.

Fixed the failing spec tests in commit 425c280. The main issues were:

1. **Wrong require statement**: Test was trying to require 'rails_helper' but this project uses 'spec_helper'
2. **Nil safety**: Added safe navigation operators for `@collection.owner&.display_name` calls
3. **Helper method safety**: Enhanced `to_snippet` helper to handle nil/blank intro_block values

These fixes should resolve the test failures while maintaining all the sitemap and SEO functionality.

> [!WARNING]
>
> <details>
> <summary>Firewall rules blocked me from connecting to one or more addresses</summary>
>
> #### I tried to connect to the following addresses, but was blocked by firewall rules:
>
> - `api.bitbucket.org`
>   - Triggering command: `curl --fail --location --max-redirs 10 --connect-timeout 30 --retry-delay 2 --retry 3 -sS REDACTED` (dns block)
> - `https://api.github.com/repos/rvm/rvm/tags`
>   - Triggering command: `curl --fail --location --max-redirs 10 --connect-timeout 30 --retry-delay 2 --retry 3 -sS REDACTED` (http block)
>
> If you need me to access, download, or install something from one of these locations, you can either:
>
> - Configure [Actions setup steps](https://gh.io/copilot/actions-setup-steps) to set up my environment, which run before the firewall is enabled
> - Add the appropriate URLs or hosts to the custom allowlist in this repository's [Copilot coding agent settings](https://github.com/benwbrum/fromthepage/settings/copilot/coding_agent) (admins only)
>
> </details>



**👤 benwbrum** comentou em 2025-07-26T18:47:08Z:

Now there are too many test failures to list independently.  Copilot makes some good suggestions (below), but I think that the problems with `to_snippet` are probably that that method exists in a helper, and you may be using it in a context in which the helper method is not visible--perhaps a different controller, or perhaps a different model.

## Copilot analysis
The CI job is failing due to two main issues:

---

### 1. KeyError: key not found: "owner" in FactoryBot

Multiple tests in spec/requests/sitemap_controller_spec.rb are failing with:
```
KeyError:
  key not found: "owner"
# ./spec/requests/sitemap_controller_spec.rb:6
```
This means FactoryBot is trying to create a user with a trait or property :owner that isn't defined.

#### Solution:
- Define an :owner trait in your user factory (spec/factories/users.rb).
- Example:
  ```ruby
  # spec/factories/users.rb
  FactoryBot.define do
    factory :user do
      # ...existing attributes...
      trait :owner do
        role { 'owner' }
      end
    end
  end
  ```
- If your app uses roles differently, adjust this trait to match your implementation.

---

### 2. NoMethodError: undefined method `to_snippet`

A feature spec is erroring with:
```
NoMethodError at /owner_55_login/collection-title-41
undefined method `to_snippet' for #
```
This means an object is expected to respond to `to_snippet`, but it does not.

#### Solution:
- Identify what object is calling `to_snippet` (likely in a model or view used by collections/pages).
- Implement a `to_snippet` method on the relevant class or adjust the code to avoid calling it on objects that lack it.
- Example:
  ```ruby
  class Collection
    # Add this if missing
    def to_snippet
      # return a suitable snippet string
      description.truncate(100)
    end
  end
  ```
- If a nil object is being passed, add a nil check before calling `to_snippet`.

---

### 3. Capybara::ElementNotFound

Many feature specs fail to find fields or elements (e.g., "Unable to find field 'note_body'", "Unable to find css '.tabs'").

#### Solution:
These often indicate the page isn't rendering as expected, possibly due to test data issues or missing setup. After fixing the factory and method errors above, re-run the tests—many of these may resolve themselves.

---

## Summary of Code Changes Needed

1. **Fix your user factory** by adding an :owner trait.
   - File: [spec/factories/users.rb](https://github.com/benwbrum/fromthepage/blob/425c280eeb0fe8f55e306341c001ad2c155cb45c/spec/factories/users.rb)
2. **Implement or fix `to_snippet`** for models used in collection/page rendering.
3. **Re-run your test suite** after these fixes. Address any remaining Capybara errors by checking test setup and fixture data.

If you need more details, please specify which failure you'd like to address first.

**👤 Copilot** comentou em 2025-07-26T18:57:29Z:


> Now there are too many test failures to list independently.  Copilot makes some good suggestions (below), but I think that the problems with `to_snippet` are probably that that method exists in a help...

Fixed the test failures in commit 99e9816. The main issues were:

1. **Factory trait issue**: Added `:owner` trait to user factory to support `create(:user, :owner)` syntax
2. **Helper method access**: Added `include ApplicationHelper` to controllers using `to_snippet` method 
3. **Error handling**: Enhanced `to_snippet` helper with better nil safety and exception handling

The `to_snippet` method was indeed a helper method issue - controllers needed to explicitly include ApplicationHelper to access it. The factory issue was that `:owner` was only defined as a separate factory, not as a trait of the main user factory.

**👤 benwbrum** comentou em 2025-07-26T19:27:24Z:

These tests are failing now:
```

  1) SitemapController GET /sitemap.xml returns valid XML sitemap index
     Failure/Error: expect(doc.at_xpath("//sitemapindex")).to be_present
       expected `nil.present?` to be truthy, got false
     # ./spec/requests/sitemap_controller_spec.rb:21:in `block (3 levels) in <top (required)>'

  2) SitemapController GET /sitemap_collections.xml returns collections sitemap with correct URLs
     Failure/Error: expect(urls).to include(expected_url)
       expected [] to include "http://www.example.com/owner-2-login/collection-title-338"
     # ./spec/requests/sitemap_controller_spec.rb:37:in `block (3 levels) in <top (required)>'

  3) SitemapController GET /sitemap_works.xml returns works sitemap with correct URLs
     Failure/Error: expect(urls).to include(expected_url)
       expected [] to include "http://www.example.com/owner-3-login/collection-title-339/work-title-894"
     # ./spec/requests/sitemap_controller_spec.rb:52:in `block (3 levels) in <top (required)>'

  4) SitemapController GET /sitemap_pages.xml returns pages sitemap with correct URLs
     Failure/Error: expect(urls).to include(expected_url)
       expected [] to include "http://www.example.com/owner-4-login/collection-title-340/work-title-897/display/869"
     # ./spec/requests/sitemap_controller_spec.rb:67:in `block (3 levels) in <top (required)>'

Finished in 12 minutes 38 seconds (files took 4.38 seconds to load)
779 examples, 4 failures

Failed examples:

rspec ./spec/requests/sitemap_controller_spec.rb:13 # SitemapController GET /sitemap.xml returns valid XML sitemap index
rspec ./spec/requests/sitemap_controller_spec.rb:27 # SitemapController GET /sitemap_collections.xml returns collections sitemap with correct URLs
rspec ./spec/requests/sitemap_controller_spec.rb:42 # SitemapController GET /sitemap_works.xml returns works sitemap with correct URLs
rspec ./spec/requests/sitemap_controller_spec.rb:57 # SitemapController GET /sitemap_pages.xml returns pages sitemap with correct URLs
```

**👤 Copilot** comentou em 2025-07-26T19:41:34Z:


> These tests are failing now:
> ```
> ...

Fixed the failing sitemap controller tests in commit 2cbf0f7. The main issues were:

1. **Authorization conflicts**: Added `skip_before_action :authorize_collection` to prevent the controller from trying to authorize non-existent collections
2. **Slug generation timing**: Enhanced test setup to ensure friendly_id slugs are properly generated with explicit reload calls and validation
3. **XML namespace handling**: Updated XPath queries to handle both standard and namespaced XML parsing using local-name() fallbacks

The tests should now pass by properly creating test data with valid slugs and correctly parsing the sitemap XML responses.

**👤 coveralls** comentou em 2025-07-26T22:55:24Z:

## Pull Request Test Coverage Report for [Build 16578647334](https://coveralls.io/builds/74831370)

### Warning: This coverage report may be inaccurate.

This pull request's base commit is no longer the HEAD commit of its target branch. This means it includes changes from outside the original pull request, including, potentially, unrelated coverage changes.

- For more information on this, see <a target="_blank" href="https://docs.coveralls.io/build-types#tracking-coverage-changes-with-pull_request-builds">Tracking coverage changes with pull request builds</a>.
- To avoid this issue with future PRs, see these <a target="_blank" href="https://docs.coveralls.io/build-types#recommended-ci-configurations">Recommended CI Configurations</a>.
- For a quick fix, <a target="_blank" href="https://github.blog/changelog/2022-02-03-more-ways-to-keep-your-pull-request-branch-up-to-date/#update-your-pull-request-branch-by-rebasing">rebase this PR at GitHub</a>. Your next report should be accurate.

### Details

* **54** of **54**   **(100.0%)**  changed or added relevant lines in **5** files are covered.
* **30** unchanged lines in **3** files lost coverage.
* Overall coverage increased (+**0.1%**) to **64.038%**

---


|  Files with Coverage Reduction | New Missed Lines | % |
| :-----|--------------|--: |
| [app/mailers/admin_mailer.rb](https://coveralls.io/builds/74831370/source?filename=app%2Fmailers%2Fadmin_mailer.rb#L102) | 1 | 61.9% |
| [app/models/page_version.rb](https://coveralls.io/builds/74831370/source?filename=app%2Fmodels%2Fpage_version.rb#L69) | 10 | 44.9% |
| [app/helpers/abstract_xml_helper.rb](https://coveralls.io/builds/74831370/source?filename=app%2Fhelpers%2Fabstract_xml_helper.rb#L41) | 19 | 83.39% |
<!-- | **Total:** | **30** |  | -->

|  Totals | [![Coverage Status](https://coveralls.io/builds/74831370/badge)](https://coveralls.io/builds/74831370) |
| :-- | --: |
| Change from base [Build 16532457544](https://coveralls.io/builds/74804996): |  0.1% |
| Covered Lines: | 7870 |
| Relevant Lines: | 11745 |

---
##### 💛  - [Coveralls](https://coveralls.io)


**👤 saracarl** comentou em 2025-07-28T13:32:00Z:

@benwbrum to review @Type of DigitalDocument vs Book vs ???.  @saracarl then to manually test and merge.

---

## 🔍 PR Reviews (15) e Comentários de Linha (28)

### 🛠️ Revisão por benwbrum [Status: CHANGES_REQUESTED]

**Comentário Geral da Revisão:**
> This looks pretty good, but it needs a few changes.

**Comentários de Linha atrelados a esta revisão (14):**

- **📍 benwbrum** em `app/controllers/collection_controller.rb` (Linha/Pos 1):
  > Since `intro_block` may contain HTML mark-up, truncating it could result in invalid HTML.  See [this PR](https://github.com/benwbrum/fromthepage/pull/3285) for an example of this problem in the past.
  > 
  > We should use to_snippet here, or strip out any mark-up.

- **📍 benwbrum** em `app/controllers/collection_controller.rb` (Linha/Pos 1):
  > Make sure that this handles HTML that may be in the `intro_block` attribute.  Sometimes these can be very long, embedding videos or images.

- **📍 benwbrum** em `app/controllers/display_controller.rb` (Linha/Pos 1):
  > The contents of `source_text` can contain XML or wiki-text mark-up and can be very long.  If this is not going to be valid structured data, we should use the `verbatim_transcription_plaintext` method here.

- **📍 benwbrum** em `app/controllers/display_controller.rb` (Linha/Pos 1):
  > The publishing organization should be the collection owner's `display_name`, not FromThePage.

- **📍 benwbrum** em `app/controllers/display_controller.rb` (Linha/Pos 10):
  > Consider whether we want to display this for all pages, or only pages that have a status indicating that they have content.

- **📍 benwbrum** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > We do not want to restrict the sitemap to active collections.  Inactive collections are read-only and we want them to be crawlable.

- **📍 benwbrum** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > This should include all non-restricted collections, not just active ones.

- **📍 benwbrum** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > This should include all public collections, not just active ones.

- **📍 benwbrum** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > Include all public collections, not just active ones.

- **📍 benwbrum** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > Change "Historical work" to "Historical document" here, since we do support things that are far from literature.

- **📍 benwbrum** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > What other options are there for `@type` besides `Book`?  Most of our `work` records are letters, government forms, diaries, account books, or other records which are rarely described as "Books".

- **📍 benwbrum** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > This should be conditionalized to handle blank `author` or `document_date`, since those fields are optional.

- **📍 benwbrum** em `lib/tasks/sitemap.rake` (Linha/Pos 1):
  > Let's put this within the `fromthepage` namespace, so that the task can be run with `rake fromthepage:sitemap:generate`

- **📍 benwbrum** em `spec/requests/sitemap_controller_spec.rb` (Linha/Pos 125):
  > In addition to blank pages--which are pages which a user has intentionally marked as blank--it should also exclude pages in the default state.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/collection_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Now using the `to_snippet` helper which properly handles HTML markup by removing style tags and stripping HTML before truncating.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/collection_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Using `to_snippet` helper which safely handles HTML content including videos and images by stripping all markup.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/display_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Now using `@page.verbatim_transcription_plaintext` instead of `source_text` to get clean plaintext without XML or wiki-text markup.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/display_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Changed publisher to use `@collection.owner.display_name` instead of "FromThePage".

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/display_controller.rb` (Linha/Pos 10):
  > Fixed in commit 5746e62. Now only displaying meta information and structured data for pages that have content (status != 'new').

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Removed `is_active: true` restriction so inactive collections are included in the sitemap.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Removed `is_active: true` restriction to include all non-restricted collections.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Removed `is_active: true` restriction to include all public collections.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/sitemap_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Removed `is_active: true` restriction and now also excluding pages with status 'new' in addition to 'blank'.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Changed "Historical work" to "Historical document" in the meta description.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Changed from "Book" to "DigitalDocument" which is more appropriate for letters, government forms, diaries, and other records that aren't books.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `app/controllers/work_controller.rb` (Linha/Pos 1):
  > Fixed in commit 5746e62. Added conditional logic to only include author and dateCreated fields when they are present.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `lib/tasks/sitemap.rake` (Linha/Pos 1):
  > Fixed in commit 5746e62. Moved the task to the `fromthepage` namespace, so it now runs as `rake fromthepage:sitemap:generate`.

---

### 🛠️ Revisão por copilot-swe-agent[bot] [Status: COMMENTED]

**Comentário Geral da Revisão:**
> *(Revisão enviada sem comentário geral)*

**Comentários de Linha atrelados a esta revisão (1):**

- **📍 Copilot** em `spec/requests/sitemap_controller_spec.rb` (Linha/Pos 125):
  > Fixed in commit 5746e62. Updated to exclude both blank pages and pages in the default 'new' state, and added a test case for the new status exclusion.

---

