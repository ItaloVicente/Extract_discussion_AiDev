# 🔍 Clone Analysis | Project: roast | PR: #244

- **Commit SHA:** `8619da128a1cc056236ea0ecd5dc5918b915d933`
- **Clone Fingerprint:** `6861dae8c39fa6e664e1749744997b1b`
- **Categoria:** `mei`

---

## 🧑‍💻 Clone Par 1
**File:** `lib/roast/workflow/configuration_loader.rb`
**Lines:** 173 to 183

```text
def display_warnings(warnings)
          return if warnings.empty?

          ::CLI::UI::Frame.open("Validation Warnings", color: :yellow) do
            warnings.each do |warning|
              puts ::CLI::UI.fmt("{{yellow:#{warning[:message]}}}")
              puts ::CLI::UI.fmt("  {{gray:→ #{warning[:suggestion]}}}") if warning[:suggestion]
              puts
            end
          end
        end
```

---

## 🧑‍💻 Clone Par 2
**File:** `lib/roast/workflow/validation_command.rb`
**Lines:** 145 to 153

```text
def display_warnings(warnings)
        ::CLI::UI::Frame.open("Warnings", color: :yellow) do
          warnings.each do |warning|
            puts ::CLI::UI.fmt("{{yellow:• #{warning[:message]}}}")
            puts ::CLI::UI.fmt("  {{gray:→ #{warning[:suggestion]}}}") if warning[:suggestion]
            puts
          end
        end
      end
```

