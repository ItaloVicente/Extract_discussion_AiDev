# 🔍 Clone Analysis | Project: json-io | PR: #382

- **Commit SHA:** `6a46d000d3df6699fc5b44cbcf7148544d2e4468`
- **Clone Fingerprint:** `b10a462aa4ae87f614097ba5f14c5b6a`
- **Categoria:** `unique_final`

---

## 🧑‍💻 Clone Par 1
**File:** `src/test/java/com/cedarsoftware/io/TestUtil.java`
**Lines:** 151 to 163

```text
private static TestInfo readGson(String json) {
        TestInfo testInfo = new TestInfo();
        try {
            Gson gson = new Gson();
            long start = System.nanoTime();
            Map<?, ?> map = gson.fromJson(json, Map.class);
            testInfo.nanos = System.nanoTime() - start;
            testInfo.obj = map;
        } catch (Exception e) {
            testInfo.t = e;
        }
        return testInfo;
    }
```

---

## 🧑‍💻 Clone Par 2
**File:** `src/test/java/com/cedarsoftware/io/TestUtil.java`
**Lines:** 165 to 177

```text
private static TestInfo readJackson(String json) {
        TestInfo testInfo = new TestInfo();
        try {
            ObjectMapper mapper = new ObjectMapper();
            long start = System.nanoTime();
            Map<?, ?> map = mapper.readValue(json, Map.class);
            testInfo.nanos = System.nanoTime() - start;
            testInfo.obj = map;
        } catch (Exception e) {
            testInfo.t = e;
        }
        return testInfo;
    }
```

