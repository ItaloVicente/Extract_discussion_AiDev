# 🔍 Clone Analysis | Project: java-util | PR: #173

- **Commit SHA:** `04fb4648f9a1f40cfff4ddeb66b47d91a36d9dd2`
- **Clone Fingerprint:** `3f48189f806fd310e50d0c6e5c47b545`
- **Categoria:** `unique_ini`

---

## 🧑‍💻 Clone Par 1
**File:** `src/main/java/com/cedarsoftware/util/AbstractConcurrentNullSafeMap.java`
**Lines:** 304 to 322

```text
public Iterator<K> iterator() {
                Iterator<Object> it = internalKeys.iterator();
                return new Iterator<K>() {
                    @Override
                    public boolean hasNext() {
                        return it.hasNext();
                    }

                    @Override
                    public K next() {
                        return unmaskNullKey(it.next());
                    }

                    @Override
                    public void remove() {
                        it.remove();
                    }
                };
            }
```

---

## 🧑‍💻 Clone Par 2
**File:** `src/main/java/com/cedarsoftware/util/ConcurrentNavigableMapNullSafe.java`
**Lines:** 319 to 337

```text
public Iterator<K> iterator() {
            Iterator<Object> it = internalKeys.iterator();
            return new Iterator<K>() {
                @Override
                public boolean hasNext() {
                    return it.hasNext();
                }

                @Override
                public K next() {
                    return owner.unmaskNullKey(it.next());
                }

                @Override
                public void remove() {
                    it.remove();
                }
            };
        }
```

