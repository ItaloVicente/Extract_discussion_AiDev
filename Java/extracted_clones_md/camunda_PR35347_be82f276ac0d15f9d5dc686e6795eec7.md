# 🔍 Clone Analysis | Project: camunda | PR: #35347

- **Commit SHA:** `3c7dcdb1f71fbaa6ac525b1ef982e3bb9d8f29d8`
- **Clone Fingerprint:** `be82f276ac0d15f9d5dc686e6795eec7`
- **Categoria:** `ini_mei`

---

## 🧑‍💻 Clone Par 1
**File:** `optimize/backend/src/test/java/io/camunda/optimize/rest/security/oauth/OptimizeRoleValidatorTest.java`
**Lines:** 121 to 133

```text
void shouldFailWhenOrganizationClaimIsEmpty() {
    // given
    final Jwt token = mock(Jwt.class);
    final Map<String, Object> claims = new HashMap<>();
    claims.put("https://camunda.com/orgs", Collections.emptyList());
    when(token.getClaims()).thenReturn(claims);

    // when
    final OAuth2TokenValidatorResult result = validator.validate(token);

    // then
    assertTrue(result.hasErrors());
  }
```

---

## 🧑‍💻 Clone Par 2
**File:** `optimize/backend/src/test/java/io/camunda/optimize/rest/security/oauth/OptimizeRoleValidatorTest.java`
**Lines:** 136 to 148

```text
void shouldFailWhenOrganizationClaimHasInvalidStructure() {
    // given
    final Jwt token = mock(Jwt.class);
    final Map<String, Object> claims = new HashMap<>();
    claims.put("https://camunda.com/orgs", "invalid-structure");
    when(token.getClaims()).thenReturn(claims);

    // when
    final OAuth2TokenValidatorResult result = validator.validate(token);

    // then
    assertTrue(result.hasErrors());
  }
```

