# 🔍 Clone Analysis | Project: core | PR: #32561

- **Commit SHA:** `12e2030d4bfde72e976d1cfa64cbfd1e2fbbc186`
- **Clone Fingerprint:** `7599f8fb1a4121ade68096b0f333d1f4`
- **Categoria:** `unique_ini`

---

## 🧑‍💻 Clone Par 1
**File:** `dotCMS/src/test/java/com/dotcms/rest/SwaggerCompliantCumulativeTest.java`
**Lines:** 198 to 225

```text
private List<Class<?>> getClassesFromDirectory(File directory, String packageName) throws Exception {
        List<Class<?>> classes = new ArrayList<>();
        
        if (!directory.exists()) {
            return classes;
        }
        
        File[] files = directory.listFiles();
        if (files == null) {
            return classes;
        }
        
        for (File file : files) {
            if (file.isDirectory()) {
                classes.addAll(getClassesFromDirectory(file, packageName + "." + file.getName()));
            } else if (file.getName().endsWith(".class")) {
                String className = packageName + "." + file.getName().substring(0, file.getName().length() - 6);
                try {
                    Class<?> clazz = Class.forName(className);
                    classes.add(clazz);
                } catch (Exception e) {
                    // Ignore classes that can't be loaded
                }
            }
        }
        
        return classes;
    }
```

---

## 🧑‍💻 Clone Par 2
**File:** `dotCMS/src/test/java/com/dotcms/rest/RestEndpointAnnotationComplianceTest.java`
**Lines:** 762 to 789

```text
private List<Class<?>> getClassesFromDirectory(File directory, String packageName) throws Exception {
        List<Class<?>> classes = new ArrayList<>();
        
        if (!directory.exists()) {
            return classes;
        }
        
        File[] files = directory.listFiles();
        if (files == null) {
            return classes;
        }
        
        for (File file : files) {
            if (file.isDirectory()) {
                classes.addAll(getClassesFromDirectory(file, packageName + "." + file.getName()));
            } else if (file.getName().endsWith(".class")) {
                String className = packageName + "." + file.getName().replace(".class", "");
                try {
                    Class<?> clazz = Class.forName(className);
                    classes.add(clazz);
                } catch (ClassNotFoundException | NoClassDefFoundError e) {
                    // Skip classes that can't be loaded
                }
            }
        }
        
        return classes;
    }
```

