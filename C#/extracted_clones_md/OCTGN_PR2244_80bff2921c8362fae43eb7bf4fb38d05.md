# 🔍 Clone Analysis | Project: OCTGN | PR: #2244

- **Commit SHA:** `64a2c1772d50f8f4133dd470c2064b5a23dfaa40`
- **Clone Fingerprint:** `80bff2921c8362fae43eb7bf4fb38d05`
- **Categoria:** `unique_mei`

---

## 🧑‍💻 Clone Par 1
**File:** `octgnFX/Octgn.JodsEngine/Scripting/Versions/Script_3_1_0_2.cs`
**Lines:** 480 to 495

```text
public void GroupSetController(int id, int player)
        {
            var g = Group.Find(id);
            var p = Player.Find((byte)player);

            if (p == Player.LocalPlayer)
            {
                if (g.Controller == Player.LocalPlayer) return;
                QueueAction(() => g.TakeControl());
            }
            else
            {
                if (g.Controller != Player.LocalPlayer) return;
                QueueAction(() => g.PassControlTo(p));
            }
        }
```

---

## 🧑‍💻 Clone Par 2
**File:** `octgnFX/Octgn.JodsEngine/Scripting/Versions/Script_3_1_0_0.cs`
**Lines:** 302 to 317

```text
public void GroupSetController(int id, int player)
        {
            var g = Group.Find(id);
            var p = Player.Find((byte)player);

            if (p == Player.LocalPlayer)
            {
                if (g.Controller == Player.LocalPlayer) return;
                QueueAction(() => g.TakeControl());
            }
            else
            {
                if (g.Controller != Player.LocalPlayer) return;
                QueueAction(() => g.PassControlTo(p));
            }
        }
```

