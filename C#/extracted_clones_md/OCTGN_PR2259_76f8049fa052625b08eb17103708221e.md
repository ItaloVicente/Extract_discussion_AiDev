# 🔍 Clone Analysis | Project: OCTGN | PR: #2259

- **Commit SHA:** `04faab926bca6dff6fd0407caaff6725330b6cf0`
- **Clone Fingerprint:** `76f8049fa052625b08eb17103708221e`
- **Categoria:** `ini_mei`

---

## 🧑‍💻 Clone Par 1
**File:** `octgnFX/Octgn.JodsEngine/Networking/BinaryStubs.cs`
**Lines:** 19 to 34

```text
public void Error(string msg)
		{
			Log.Debug($"OCTGN OUT: {nameof(Error)}");
		    if(Program.Client == null)return;
			MemoryStream stream = new MemoryStream(512);
			stream.Seek(4, SeekOrigin.Begin);
			BinaryWriter writer = new BinaryWriter(stream);

			writer.Write(Program.Client.Muted);
			writer.Write((byte)0);
			writer.Write(msg);
			writer.Flush(); writer.Seek(0, SeekOrigin.Begin);
			writer.Write((int)stream.Length);
			writer.Close();
			Send(stream.ToArray());
		}
```

---

## 🧑‍💻 Clone Par 2
**File:** `octgnFX/Octgn.JodsEngine/Networking/BinaryStubs.cs`
**Lines:** 1055 to 1069

```text
public void CancelLimitedReq()
		{
			Log.Debug($"OCTGN OUT: {nameof(CancelLimitedReq)}");
		    if(Program.Client == null)return;
			MemoryStream stream = new MemoryStream(512);
			stream.Seek(4, SeekOrigin.Begin);
			BinaryWriter writer = new BinaryWriter(stream);

			writer.Write(Program.Client.Muted);
			writer.Write((byte)83);
			writer.Flush(); writer.Seek(0, SeekOrigin.Begin);
			writer.Write((int)stream.Length);
			writer.Close();
			Send(stream.ToArray());
		}
```

