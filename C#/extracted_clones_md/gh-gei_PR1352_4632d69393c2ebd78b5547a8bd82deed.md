# 🔍 Clone Analysis | Project: gh-gei | PR: #1352

- **Commit SHA:** `de24fb20a2e3cf82f548c74322e1f6a0ad4d4a7d`
- **Clone Fingerprint:** `4632d69393c2ebd78b5547a8bd82deed`
- **Categoria:** `mei_final`

---

## 🧑‍💻 Clone Par 1
**File:** `src/ado2gh/Commands/ShareServiceConnection/ShareServiceConnectionCommand.cs`
**Lines:** 40 to 57

```text
public override ShareServiceConnectionCommandHandler BuildHandler(ShareServiceConnectionCommandArgs args, IServiceProvider sp)
        {
            if (args is null)
            {
                throw new ArgumentNullException(nameof(args));
            }

            if (sp is null)
            {
                throw new ArgumentNullException(nameof(sp));
            }

            var log = sp.GetRequiredService<OctoLogger>();
            var adoApiFactory = sp.GetRequiredService<AdoApiFactory>();
            var adoApi = adoApiFactory.Create(args.AdoPat);

            return new ShareServiceConnectionCommandHandler(log, adoApi);
        }
```

---

## 🧑‍💻 Clone Par 2
**File:** `src/ado2gh/Commands/LockRepo/LockRepoCommand.cs`
**Lines:** 43 to 60

```text
public override LockRepoCommandHandler BuildHandler(LockRepoCommandArgs args, IServiceProvider sp)
        {
            if (args is null)
            {
                throw new ArgumentNullException(nameof(args));
            }

            if (sp is null)
            {
                throw new ArgumentNullException(nameof(sp));
            }

            var log = sp.GetRequiredService<OctoLogger>();
            var adoApiFactory = sp.GetRequiredService<AdoApiFactory>();
            var adoApi = adoApiFactory.Create(args.AdoPat);

            return new LockRepoCommandHandler(log, adoApi);
        }
```

