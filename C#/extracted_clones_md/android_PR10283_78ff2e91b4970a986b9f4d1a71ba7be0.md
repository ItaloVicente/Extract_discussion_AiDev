# 🔍 Clone Analysis | Project: android | PR: #10283

- **Commit SHA:** `ea6f239f845cef5f881a638c00eb45c6ef679cf9`
- **Clone Fingerprint:** `78ff2e91b4970a986b9f4d1a71ba7be0`
- **Categoria:** `mei_final`

---

## 🧑‍💻 Clone Par 1
**File:** `src/Xamarin.Android.Build.Tasks/Tasks/CheckForInvalidDesignerConfig.cs`
**Lines:** 46 to 56

```text
static bool HasResourceDesignerAssemblyReference (MetadataReader reader)
		{
			foreach (var handle in reader.AssemblyReferences) {
				var reference = reader.GetAssemblyReference (handle);
				var name = reader.GetString (reference.Name);
				if (string.CompareOrdinal (name, "_Microsoft.Android.Resource.Designer") == 0) {
					return true;
				}
			}
			return false;
		}
```

---

## 🧑‍💻 Clone Par 2
**File:** `src/Xamarin.Android.Build.Tasks/Utilities/MonoAndroidHelper.cs`
**Lines:** 333 to 343

```text
public static bool HasMonoAndroidReference (MetadataReader reader)
		{
			foreach (var handle in reader.AssemblyReferences) {
				var reference = reader.GetAssemblyReference (handle);
				var name = reader.GetString (reference.Name);
				if ("Mono.Android" == name) {
					return true;
				}
			}
			return false;
		}
```

